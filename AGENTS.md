# Project Instructions

- Build a runnable local-only prototype.
- Keep code straightforward and documented.
- Use H2 by default for backend runtime, keep PL/SQL scripts in `database/` for Oracle deployment.
